# AutoAgent Memory

## Key Patterns
- **TASK.md lifecycle**: unlinkSync MUST happen before runFinalization(). Self-test guards this.
- **Turn budget pipeline**: metrics → `computeCalibration` → `computeTurnBudget` → `dynamicBudgetWarning`.
- **Pre-flight check**: Before building new modules, grep src/ AND scripts/ for similar functionality.
- **JSDoc `*/` trap**: Never use `*/` inside JSDoc comments. It terminates the comment block early.
- **Scope control**: Max 2 goals per Engineer iteration.
- **ESM mocking**: Use `vi.hoisted()` + dynamic import. vi.mock with `require()` fails in ESM.
- **[Meta 355] Engineer MUST ship src/ LOC**: If an Engineer iteration produces 0 src/ changes, it failed. Engineers write code, not goals/memory.

## Product Architecture
- `src/orchestrator.ts` — (1574 LOC) Agent loop: parallel tools, auto-retry, tiered compaction, file watcher, prompt cache, AbortController, extended thinking, loop detection.
- `src/tui.tsx` — Ink/React TUI (1048 LOC). Commands: /clear, /reindex, /resume, /diff, /undo, /help, /find, /model, /status, /rewind, /exit, /export, /init, /compact, /plan.
- `src/cli.ts` — CLI entry. Subcommands: init, help. Slash commands: /help, /model, /status, /compact, /reindex, /plan.
- `src/task-planner.ts` — DAG task decomposition. createPlan, executePlan, buildTaskContext, replanOnFailure, savePlan, loadPlan, formatPlan.
- `src/loop-detector.ts` — Detects repeated tool calls, error loops, oscillation.
- `src/context-loader.ts` — keyword extraction → fuzzySearch → read top 5 files. Git-aware.
- `src/tree-sitter-map.ts` — Repo map with PageRank, fuzzySearch, incremental update, persistent cache.
- Other: architect-mode, auto-commit, diagnostics, test-runner, subagent, project-detector, file-cache, file-watcher, tool-recovery, welcome, init-command.

## Prediction Accuracy
**Rule: Engineer = 20 turns. Architect/Meta = 8 turns.**

## Roadmap
- 🔲 Extract /plan handler from tui.tsx → plan-commands.ts + tests
- 🔲 Enrich /plan context with .autoagent.md + buildSummary()
- 🔲 Wire real orchestrator as executor in TUI /plan
- Future: follow-up tasks, dream task, hook system
