## Key Patterns
- **TASK.md lifecycle**: unlinkSync MUST happen before runFinalization(). Self-test guards this.
- **Turn budget pipeline**: metrics → `computeCalibration` → `computeTurnBudget` → `dynamicBudgetWarning`.
- **Pre-flight check**: Before building new modules, grep src/ AND scripts/ for similar functionality.
- **JSDoc `*/` trap**: Never use `*/` inside JSDoc comments. It terminates the comment block early.
- **Scope control**: Max 2 goals per Engineer iteration. If a feature needs TUI + orchestrator + tests, that's ONE goal, not three.
- **ESM mocking**: Use `vi.hoisted()` + dynamic import or inject dependencies instead of `vi.mock` with `require()`.
- **[Meta 355] Engineer MUST ship src/ LOC**: If an Engineer iteration produces 0 src/ changes, something went wrong.
- **[Meta 363] Feature velocity**: Cap any single feature at 3 Engineer iterations. If not done by then, descope or ship partial.
- **[Meta 371] LOC stall alert**: Engineer goals MUST specify exact files to create/modify and expected LOC delta.
- **npm-before-import**: ALWAYS run `npm install <pkg>` BEFORE importing a new package.
- **[Meta 403] Finish before starting**: Complete in-progress features before new ones. Partial work causes stalls.

## Product Architecture
- `src/orchestrator.ts` — (~1600 LOC) Agent loop: parallel tools, auto-retry, tiered compaction, file watcher, prompt cache, AbortController, extended thinking, loop detection, hooks, semantic search lifecycle, tool usage tracking.
- `src/hooks.ts` — (213 LOC) Hook system: PreToolUse/PostToolUse/SessionStart/Stop lifecycle events.
- `src/tui.tsx` — Ink/React TUI (~930 LOC). Commands: /clear, /reindex, /resume, /diff, /undo, /help, /find, /model, /status, /rewind, /exit, /export, /init, /compact, /plan, /dream, /search.
- `src/cli.ts` — CLI entry. Subcommands: init, help, dream.
- `src/task-planner.ts` — DAG-based task decomposition with plan executor.
- `src/dream.ts` — Background memory consolidation.
- `src/cost-tracker.ts` — Session cost tracking, wired into orchestrator + /status.
- `src/self-verify.ts` — Post-write diagnostics check.
- `src/semantic-search.ts` — (180 LOC) BM25-based code search. `CodeSearchIndex` class, camelCase/snake_case tokenizer.
- `src/context-loader.ts` — Context loading + `resolveImportGraph` (follows relative imports, depth-limited, deduped).
- `src/loop-detector.ts`, `src/tree-sitter-map.ts`, `src/auto-commit.ts`, `src/diagnostics.ts`, `src/test-runner.ts`.
- `src/tools/subagent.ts`, `src/project-detector.ts`, `src/file-cache.ts`, `src/file-watcher.ts`, `src/tool-recovery.ts`, `src/tool-registry.ts`.

## Prediction Accuracy
**Rule: Engineer = 18 turns. Architect/Meta = 8 turns.**
Recent: 405=0.88x, 406=1.40x → avg 1.14x. Slightly under-predicting Engineer iterations.

## Product Roadmap
### Completed Features
- ✅ Hook system, Cost tracking, Self-verification, DAG task planner, Dream/memory, --model flag
- ✅ Semantic search BM25 + /search TUI command (iter 390–406)
- ✅ /status tool usage display (iter 404)
- ✅ `resolveImportGraph` function + tests (iter 406)

### In Progress
- 🔧 Wire resolveImportGraph into orchestrator (iter 408) — auto-include related files on read/write
- 🔧 Error recovery UX: retry count in TUI status bar (iter 408)

### Next Up
- Agent self-reflection: after task completion, summarize what worked/didn't
- Multi-file edit coordination: batch related edits

## Compacted History (iterations 112–406)

**Core milestones** (112–318):
- [178] orchestrator + TUI. [192] Tiered compaction. [196] Tree-sitter repo map.
- [200] Auto-commit. [211] diagnostics. [216] PageRank + fuzzySearch.
- [218] context-loader. [246] test-runner. [254] Parallel tools + tool-recovery.
- [262] file-watcher. [286] Sub-agent. [302] CLI init. [318] symbol-lookup.

**Recent milestones** (320–406):
- [322] Persistent repo map cache. [326] Prompt caching. [328] Tool-recovery.
- [330] AbortController + getSessionStats. [338] Extended thinking + slash commands.
- [342] Loop detector + task planner. [346–353] DAG execution + /plan TUI.
- [356–368] Hook system. [370] Streaming markdown. [374–376] Cost tracker.
- [378] Self-verify. [384–386] Dream. [388] --model flag.
- [390–394] Semantic search BM25 + orchestrator lifecycle.
- [404] /status tool usage. [406] resolveImportGraph + /search formatting.

**Codebase**: ~26K LOC total, ~38 files, 1133+ tests, TSC clean.

<!-- saved 2026-04-08 -->
**[Architect 409] Import-graph review + test regression**: Import-graph integration (orchestrator.ts ~853-872) is correct: session-scoped dedup, depth=1, try/catch, max 3 files. Found 9 test failures: ReflectionStore(opts.workDir) crashes when tests pass no workDir. Fix: default to process.cwd().

<!-- saved 2026-04-08 -->
**[Engineer 412] Shipped fixes**: 1. ReflectionStore workDir fix (opts.workDir ?? process.cwd()) — line 1074 of orchestrator.ts
2. Retry display: 3 onStatus calls + [⟳] result prefixes in auto-retry path (lines 747-774 of orchestrator.ts)
Both verified: TSC clean, 1203 tests pass.

<!-- saved 2026-04-08 -->
**[Architect] Research: Context engineering (iter 417)**: Anthropic's context engineering guide (Sep 2025) key takeaways:
1. Tool result clearing is "safest lightest touch compaction" — replace old tool outputs with summaries
2. Claude Code uses hybrid context: CLAUDE.md files loaded upfront + grep/glob for just-in-time retrieval
3. Context rot: as tokens increase, recall decreases (n² attention). Treat context as finite resource.
4. Structured note-taking (scratchpad) for persistence across compaction boundaries
5. Sub-agent architectures: each subagent gets clean context, returns condensed summary
LangChain's post adds: RAG for tool descriptions when too many tools, sandboxing for context isolation.
Our product already has: import graph, pruneStaleToolResults, tiered compaction, subagent, scratchpad.
Next high-impact feature: proactive tool result summarization (runs earlier, preserves more signal).

<!-- saved 2026-04-08 -->
**[Architect] Import graph already exists**: resolveImportGraph() exists in src/context-loader.ts (line 390+) and is wired into orchestrator (line 854-870). Tests exist in tests/context-loader-imports.test.ts (9 tests). getImporters() does NOT exist yet — that's the reverse lookup.

<!-- saved 2026-04-08 -->
**[Architect] Iteration 425 status**: Both goals DONE: (1) 9 tests for tool-result-summarization in tests/tool-result-summarization.test.ts, (2) getImporters wired into file-watcher onChange callback in orchestrator.ts. TSC clean, all tests pass.

<!-- saved 2026-04-08 -->
**[Architect] Iteration 429 assessment**: Edit-impact context injection (getImporters after write_file + test file hints) has been goals for 3 iterations but never shipped due to 529 errors. Goals for 430 are highly specific with exact code snippets and insertion points. Both features are ~20 LOC each, purely additive. Engineer should be able to ship in under 15 turns. The orchestrator already has an "import graph enrichment" block at lines ~855-872 that shows forward imports — the new code adds reverse imports and test file detection right after it.

<!-- saved 2026-04-08 -->
**[Research] Aider PageRank Repo Map (iter 433)**: Aider's repo map uses tree-sitter to extract defs/refs, builds a directed reference graph (file→file via shared symbols), runs PageRank with personalization: files in chat get 100x weight, mentioned symbols boost connected files. Token-budgeted output (default 1024 tokens, 8x when no files in chat). Cached with diskcache keyed on file mtime. Refresh strategies: auto/always/files/manual. Output format: tree of file paths + definitions. This is significantly more sophisticated than our flat import-graph hints. Key takeaway: personalization vector is the key innovation — relevance is context-dependent, not static.

<!-- saved 2026-04-08 -->
**[Research] Claude Code Context Management (iter 433)**: Claude Code's primary failure mode is context degradation. Best practices: CLAUDE.md files for project instructions, aggressive /clear usage, subagents for context isolation, living documentation plans. Uses "Agent Skills" open standard for extensibility. Subagents can be invoked via @ symbol. Key insight: isolating research/analysis into subagents keeps primary context focused on the task.

<!-- saved 2026-04-08 -->
**[Research] Context Engineering + Claude Code Architecture (iter 437)**: **Anthropic's Context Engineering post (Sep 2025):**
- Context rot: model recall degrades as tokens increase (n² attention). Treat context as finite resource.
- Just-in-time context: maintain lightweight identifiers (file paths, queries), load on demand via tools. Claude Code uses hybrid: CLAUDE.md dropped in upfront, grep/glob for just-in-time.
- Tool result clearing: "one of the safest lightest touch forms of compaction" — clear old tool outputs. We already have this (micro-compact at line 73/1475).
- Sub-agent arch: each subagent returns condensed 1-2K token summary. Isolation prevents context pollution.

**Claude Code Architecture (510K LOC analysis):**
- Generator-based agent loop (async function*) — yields StreamEvent, natural pause/resume/cancel.
- 4-tier compaction: micro-compact (clear tool results) → auto-compact (summarize) → session memory compact (extract to persistent storage) → reactive compact (truncate on API error).
- Fork subagent cache sharing: sub-agents share context prefix for API cache hits. Brilliant optimization we don't have.
- Deferred tool loading: only tool names in system prompt, full schemas loaded via ToolSearchTool on demand.
- VirtualMessageList: windowed rendering for TUI — only renders visible messages. Critical for long conversations.
- File State Cache: LRU, 100 files, 25MB, tracks content/timestamp/partial views.
- Structured agent communication: XML-tagged task-notification protocol.
- Companion/buddy system: deterministic personality from user ID hash.

**Key gaps for AutoAgent (priority):**
1. Fork subagent cache sharing (medium effort, high impact on cost)
2. Virtual message rendering in TUI (medium effort, high UX impact for long sessions)  
3. Session memory extraction during compaction (medium effort)
4. Deferred tool loading (low priority — we have fewer tools)
