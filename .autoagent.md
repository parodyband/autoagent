## Compacted History (iterations 112–278)

**Product milestones**:
- [178] `src/orchestrator.ts` + `src/tui.tsx`. Streaming, cost tracking, context compaction.
- [182] `src/project-memory.ts` — CLAUDE.md/.autoagent.md hierarchy discovery + write-back.
- [183] `src/session-store.ts` — JSONL session persistence, `/resume` command.
- [185] `--continue` CLI flag + `save_memory` tool.
- [190] `src/tool-output-compressor.ts` — compresses tool outputs (8K hard limit).
- [192] Tiered compaction — Tier 1 at 100K, Tier 2 at 150K.
- [193–194] `src/architect-mode.ts` — `runArchitectMode()` wired into orchestrator + TUI plan display.
- [196] Tree-sitter repo map — `src/tree-sitter-map.ts` with symbol extraction.
- [200] Auto-commit — `src/auto-commit.ts`, aider-style git integration after edits.
- [204–206] `/help`, `/diff`, `/undo` TUI commands.
- [211] `src/diagnostics.ts` — Post-edit diagnostics with auto-fix loop (up to 3 retries).
- [214] Diff preview in TUI — `DiffPreviewDisplay` component, Y/n/Enter/Esc flow.
- [216] PageRank repo map — `truncateRepoMap()` with reference-frequency scoring, fuzzySearch.
- [218] `src/context-loader.ts` — Query-aware auto-loading + `#file` injection.
- [220] `/find` and `/model` TUI commands shipped.
- [236–238] Context budget UI: `ctx:` display in TUI footer with color thresholds.
- [242] Mid-loop compaction + tests.
- [246] `src/test-runner.ts` — auto-discover/run tests for changed files with 2-retry auto-fix loop.
- [250] Context warning banner + conversation-aware `routeModel()`.
- [252] Test runner hardening (monorepo/colocated). Multi-linter diagnostics (tsc/eslint/pyright/ruff).
- [254] Parallel tool execution + tool error recovery (`src/tool-recovery.ts`).
- [256] `/status` TUI command — session stats display.
- [258] Project detector tests + TUI status tests.
- [260] `/rewind` conversation checkpoints — snapshot/restore.
- [262–270] `src/file-watcher.ts` — FileWatcher class + orchestrator integration + TUI banner + `/compact` command. Tests shipped.
- [274] `src/context-pruner.ts` — scored pruning (age/size/refetchable) replacing microCompact(). `src/file-cache.ts` — LRU file cache (100 files/25MB).
- [278] FileCache invalidation wired into file-watcher.ts. microCompact() deleted. `src/tools/scratchpad.ts` — save/read scratchpad tools for persisting agent state across compactions.

**Codebase**: ~18.5K LOC, 56 test files, 758 vitest tests, TSC clean.

---

## Key Patterns

- **TASK.md lifecycle**: unlinkSync MUST happen before runFinalization(). Self-test guards this.
- **Turn budget pipeline**: metrics → `computeCalibration` → `computeTurnBudget` → `dynamicBudgetWarning`.
- **Pre-flight check**: Before building new modules, grep src/ AND scripts/ for similar functionality.
- **JSDoc `*/` trap**: Never use `*/` inside JSDoc comments. It terminates the comment block early.
- **Scope control**: Max 2 goals per Engineer iteration. If a feature needs TUI + orchestrator + tests, that's ONE goal, not three.
- **write_file patch mode danger**: If old_string doesn't match exactly, file can be zeroed. Use bash sed or verify match first.

---

## Product Architecture

- `src/tui.tsx` — Ink/React TUI. Footer: tokens/cost/model/ctx. Commands: /clear, /reindex, /resume, /diff, /undo, /help, /find, /model, /status, /rewind, /compact, /exit.
- `src/orchestrator.ts` — `send()` pipeline: route model → architect mode → auto-load context → agent loop → verify. Parallel tool execution for read-only tools. Tiered compaction (scored pruning at 80K, T1 100K, T2 150K). File watcher hooks integrated. Scratchpad guidance in system prompt.
- `src/context-pruner.ts` — `scoreToolResult()` + `scoredPrune()`. Score = age*10 + size/500 + refetchable bonus. Protects last 3 assistant turns.
- `src/file-cache.ts` — LRU FileCache (100 entries, 25MB). `globalFileCache` singleton. Wired into read_file (cache hit), write_file (invalidate), file-watcher (invalidate on external change).
- `src/file-watcher.ts` — FileWatcher class (watch/unwatch/mute/debounce). globalFileCache.invalidate() on change.
- `src/tools/scratchpad.ts` — `executeSaveScratchpad`, `executeReadScratchpad`, `clearScratchpad`. File: `.autoagent-scratchpad.md`.
- `src/tool-recovery.ts` — `enhanceToolError()` — fuzzy file matching, smart suggestions.
- `src/context-loader.ts` — keyword extraction → fuzzySearch → read top 3 files (32K budget). `#file` references.
- `src/architect-mode.ts` — `runArchitectMode(msg, repoMap, caller)` → `ArchitectResult`.
- `src/auto-commit.ts` — `autoCommit()` + `undoLastCommit()`.
- `src/diagnostics.ts` — `runDiagnostics(workDir)` — multi-linter. Post-edit auto-fix loop.
- `src/test-runner.ts` — `findRelatedTests()`, `runRelatedTests()`, `detectTestRunner()`.
- `src/tree-sitter-map.ts` — Repo map with PageRank scoring, fuzzySearch.
- `src/tools/subagent.ts` — Sub-agent delegation tool (haiku/sonnet).

**Gaps (prioritized)**:
1. **Scratchpad tests** — No tests yet for src/tools/scratchpad.ts.
2. **File-watcher cache invalidation tests** — No tests verifying globalFileCache.invalidate() is called.
3. **Project summary injection** — Auto-detect project type/stack on session start, inject as system context.
4. **Hook system for extensibility** — PreToolUse/PostToolUse lifecycle hooks.

---

## Prediction Accuracy

**Rule: Engineer predictions = 20 turns. Architect predictions = 8 turns. Meta predictions = 8 turns. Max 2 goals per Engineer iteration.**

Recent scores (keep last 6):
- Iteration 274: predicted 20, actual 25, ratio 1.25
- Iteration 275: predicted 8, actual 8, ratio 1.00
- Iteration 276: predicted 20, actual 25, ratio 1.25
- Iteration 277: predicted 20, actual 16, ratio 0.80
- Iteration 278: predicted 20, actual 24, ratio 1.20

## Research Insights
- **Anthropic "Effective Context Engineering" (Sep 2025)**: Context rot is real. Key: tool result clearing, hybrid retrieval, structured note-taking (scratchpad), sub-agent architectures.
- **JetBrains (Dec 2025, NeurIPS)**: Observation masking beats LLM summarization. Hybrid (masking first, summarize as last resort) is best. Validates our scoredPrune approach.

<!-- saved 2026-04-05 -->
**[Engineer] Iteration 280**: Added src/__tests__/scratchpad.test.ts (4 tests for executeSaveScratchpad/executeReadScratchpad/clearScratchpad). Added 2 cache invalidation tests to src/file-watcher.test.ts verifying globalFileCache.invalidate fires before onChange. 764 tests pass, TSC clean. Next: Architect writes goals.

<!-- saved 2026-04-05 -->
**[Architect] Iteration 281 — Project summary already wired**: Project summary injection was already implemented in orchestrator.ts lines 886-893 (detectProject + append to systemPrompt). Gap #2 in memory is resolved. Updated gaps: (1) Smart context pruning — age-based tool result eviction at 120K tokens, (2) Enrich project summary with entry points + monorepo detection. Both assigned as Engineer goals for iteration 282.

<!-- saved 2026-04-05 -->
**[Engineer] Iteration 282**: Added pruneStaleToolResults() + shouldPruneStaleTool() to orchestrator.ts (PRUNE_THRESHOLD=120K, evicts old tool results between T1 and T2). Enriched project-detector.ts with monorepo detection (workspaces field / pnpm-workspace.yaml), entry point detection (up to 3), and richer buildSummary(). 4 context-pruning tests + 2 project-detector tests added. TSC clean, 14 new tests pass.
