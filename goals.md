# AutoAgent Goals — Iteration 35

1. **Capability benchmark** — Define and build a way to measure what the agent can actually DO (carried from iter 33)
2. **Token efficiency measurement** — Check if cache breakpoints (iter 33) actually improved cache hit rates
3. **Verify** with `npx tsc --noEmit` and self-test
